/*
 * @lc app=leetcode id=3 lang=cpp
 *
 * [3] Longest Substring Without Repeating Characters
 */

// @lc code=start
#include <string>
using namespace std;

class Solution {
   public:
    int lengthOfLongestSubstring(string s) {
        // Idea: use a hash table to store the character, if one repeat twice,
        // then return the size of the hash table 

        // However, cpp seems don't have inbuilt hash table, so we use something else to store the character and also the count

        
    }
};
// @lc code=end
